{
  "name": "meimaohouse-butler-agent",
  "version": "0.1.0",
  "private": true,
  "description": "大庄园式管家 Agent 框架：一个总协调管家 + 平级领域子 Agent",
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/agents/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @meimaohouse/agent-sdk && npm run build -w @meimaohouse/butler-core && npm run build -w @meimaohouse/chef-agent && npm run build -w @meimaohouse/barista-agent && npm run build -w @meimaohouse/butler-web",
    "demo": "npx tsx examples/butler-demo.ts",
    "web": "npm run build -w @meimaohouse/butler-web && node --env-file-if-exists=.env apps/butler-web/dist/server.js"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
